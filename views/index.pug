extends layout

block content
    if users.length
        table
            tr
                th Name
            each user in users
                tr
                    td= user.name
    else
        p No users yet :(